# Локальные правила проекта

## Способы подключения к 1С (приоритет — в этом порядке)

### 1. WSL-мост к Windows-1С 8.3.27 (ОСНОВНОЙ, успешный)
Последний подтверждённый способ работы с 1С — запуск Windows-приложений из WSL напрямую через WSL-interop. НЕ через `cmd.exe`/`cmd /c` — он искажает кириллицу в аргументах (cp1251). Прямой вызов `1cv8.exe` из WSL передаёт argv в UTF-16 корректно.

- Бинарь клиента: `/mnt/c/Program Files/1cv8/8.3.27.1859/bin/1cv8.exe`
- Серверная база: `_qbox\probe-1c` (172.24.159.3), пользователь `admin` без пароля.
- Платформа сервера — **8.5.1.1150** (клиент 8.3.27.1859 НЕ подходит для загрузки в конфигуратор из-за несовпадения версий).
- Стейджинг файлов: `C:\temp\...` ↔ `/mnt/c/temp/...` (пути передавать в Windows-форме).
- Типичный вызов (пример — сборка обработки):
  `"/mnt/c/Program Files/1cv8/8.3.27.1859/bin/1cv8.exe" DESIGNER /S"_qbox\probe-1c" /N"admin" /LoadExternalDataProcessorOrReportFromFiles "C:\temp\epf_src\ЗагрузкаИзТабличногоДокумента.xml" "C:\temp\probe1_epf.epf" /Out"..." /DisableStartupDialogs`
- Лицензия выдаётся сервером.
- ВАЖНО: сети между машинами «surface» флаки (Wi-Fi Direct, одноимённые хосты) — при сбое соединения повторить попытку.

## Факты о платформе (выяснено эмпирически, без ITS)

- `НачатьПомещениеФайла` на платформе 8.5.1.1150 принимает **максимум 6 параметров**:
  `(Файл, ДополнительныеПараметры, МногострочныйВыбор, ОписаниеОповещенияОЗавершении, ОписаниеОповещенияОВыборе, Заголовок)`.
  Параметры `Фильтр` и `ФормаВладелец` передавать нельзя — «Слишком много фактических параметров».

## Git-ритуал (обязательно перед каждым помещением в git)

1. Дополнить `dialogue-log.json` записями текущей сессии.
2. Пересчитать телеметрию скриптом (общее / ИИ / орг) и обновить раздел README + `users-artefacts/telemetry_chart.png`.
3. Только затем commit + push.